name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: VPS_HOST
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24.19.0
          cache: pnpm

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@latest --activate

      - run: pnpm ci
      - name: Build
        env:
          NEXT_PUBLIC_API_URL: "https://proplayas.org/api/"
          NEXT_PUBLIC_STORAGE_URL: "https://proplayas.org/storage/uploads"
          NODE_ENV: production
        run: pnpm run build

      - name: Empaquetar solo el build
        run: |
          cp -r .next/static .next/standalone/.next/static
          cp -r public .next/standalone/public
          tar -czf build.tar.gz -C .next/standalone .

      - name: Subir al VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: build.tar.gz
          target: /home/proplayas/web/proplayas.org/public_html/incoming/

      - name: Desplegar en el VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            mkdir -p /home/proplayas/web/proplayas.org/public_html/incoming/build
            tar -xzf /home/proplayas/web/proplayas.org/public_html/incoming/build.tar.gz -C /home/proplayas/web/proplayas.org/public_html/incoming/build/
            rm -rf /home/proplayas/web/proplayas.org/public_html/previous
            [ -d /home/proplayas/web/proplayas.org/public_html/current ] && mv /home/proplayas/web/proplayas.org/public_html/current /home/proplayas/web/proplayas.org/public_html/previous
            mv /home/proplayas/web/proplayas.org/public_html/incoming/build /home/proplayas/web/proplayas.org/public_html/current
            sudo systemctl restart proplayas-frontend
            sleep 3
            curl -sf http://localhost:3000 || (mv /home/proplayas/web/proplayas.org/public_html/previous /home/proplayas/web/proplayas.org/public_html/current && sudo systemctl restart proplayas-frontend && exit 1)